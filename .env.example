# Copy this file to .env.local. Use your provider's actual base URL and model.
# Secrets are read only by the local Node server. Never use VITE_ for an API key.
AI_BASE_URL=https://api.example.com/v1
AI_MODEL=your-model-name
AI_API_KEY=
